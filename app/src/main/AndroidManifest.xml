<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.ecole.eni.tplokacar">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ListeVehiculeActivity"
            android:label="@string/title_activity_liste_vehicule"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.DetailVehiculeActivity" />
        <activity
            android:name=".activity.CreateCarActivity"
            android:exported="true" />
        <activity android:name=".activity.ListeClientActivity" />
        <activity android:name=".activity.DetailClientActivity" />
        <activity android:name=".activity.CreateClientActivity"  android:exported="true">

        </activity>
    </application>

</manifest>